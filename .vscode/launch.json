{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "python",
            "name": "当前文件 (外部终端)",
            "request": "launch",
            "program": "${file}",
            "cwd": "${fileDirname}",
            "console": "externalTerminal"
        }
    ]
}